services:
  mongo:
    image: mongo:latest
    container_name: mongo
    build:
      context: .
      dockerfile: mongo/Dockerfile
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: github
    volumes:
      - ./mongo/data:/data/db

volumes:
  mongo-data:
